module.exports = {
  plugins: {
    // Permite que PostCSS procese @import en tus CSS
    'postcss-import': {},
    // Añade TailwindCSS como plugin de PostCSS
    tailwindcss: {},
    // Añade autoprefixer (generación de vendor prefixes)
    autoprefixer: {},
  },
};
